public class CollectionsFrameworkMethods {
    public static void main(String args[]){
        //methods in Collection Methods
        //1.add(object o)-->to insert an element 
        //2.addAll(collection c)--->to insert Collection of elements 
        //3.clear()--->removed all the elements in the collection
        //4.contains(object o)----->returns true if the specified object is found in the Collection
        //5.containsAll(Collection c)----->This method returns true if the collection contains all of the elements in the given collection.
        //6.equals(Object o)------->This method compares the specified object with this collection for equality.
        //7.hashCode()-------->This method is used to return the hash code value for this collection.
         //8.isEmpty()--------->This method returns true if this collection contains no elements.
         //9.iterator()--------->This method returns an iterator over the elements in this collection.
         //10.max() ----->This method is used to return the maximum value present in the collection.
         //11.parallelStream()------->This method returns a parallel Stream with this collection as its source.
         /*12.remove(Object o)-------->This method is used to remove the given object from the collection. 
                                    If there are duplicate values, then this method removes the first occurrence of the object.
          13.removeAll(Collection c)---------->This method is used to remove all the objects mentioned in the given collection from the collection.
          14.removeIf(Predicate filter)-------->This method is used to remove all the elements of this collection that satisfy the given predicate.
          15.retainAll(Collection c)------------>This method is used to retain only the elements in this collection that are contained in the specified collection.
         16.size()-------------------->This method is used to return the number of elements in the collection.
          17.spliterator()------------->This method is used to create a Spliterator over the elements in this collection.
           18.stream()-------------->This method is used to return a sequential Stream with this collection as its source.
            19.toArray()------------>This method is used to return an array containing all of the elements in this collection. */
    }
}
